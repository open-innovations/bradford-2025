# Building block data

This directory is for data that we don't otherwise have available in the repository.

Each file should also have some metadata (perhaps in a JSON file using the same file name but with `_meta` added?) so that we can, at a minimum, know the `source` and `updated` date/time.