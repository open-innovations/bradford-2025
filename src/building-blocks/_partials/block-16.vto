---
block: block-16
---

{{ comp grid.Block { block, internal, draft, id, tag: 'li', classes: 'events', width: '300px', source: 'Bradford 2025', notes: 'This number currently uses the free events listed on the internal dashboard and divides by the total events.' } }}

	<h3>Free events</h3>

	{{>
		const { events: events } = search.data("insights/dashboard");
	}}

	{{ comp.BigNumber({
		number: (100*events.free/events.total) || '&mdash;',
		suffix: '%',
		classes: 'big',
		animate: true,
		roundingOptions: { maximumFractionDigits: 1 }
	}) }}
	<p>Percentage of (public) events which were free.</p>

{{ /comp }}