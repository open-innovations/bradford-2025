{{ comp dashboard.Frame }}

    {{ comp dashboard.Cell {
        title: "Participants involved in activities",
        flash: 'Sample',
    } }}
        {{ comp.BigNumber({
            number: sample.audience["Participants involved in activities"] ,
        }) }}
        {{ comp Controls }}
            {{ comp Popover }}
                TBC
            {{ /comp }}
        {{ /comp }}
    {{ /comp }}

    {{ comp dashboard.Cell {
        title: "Schools engaged",
        flash: 'Sample',
    } }}
        {{ comp.BigNumber({
            number: sample.audience["Schools engaged"] ,
        }) }}
        {{ comp Controls }}
            {{ comp Popover }}
                TBC
            {{ /comp }}
        {{ /comp }}
    {{ /comp }}

    {{ comp dashboard.Cell {
        title: "Participants engaged in creative health activities",
        flash: 'Sample',
    } }}
        {{ comp.BigNumber({
            number: sample.audience["Participants engaged in creative health activities"],
        }) }}
        {{# Estelle - creative health manager. How this data logged. John McMahon #}}
        {{ comp Controls }}
            {{ comp Popover }}
                TBC
            {{ /comp }}
        {{ /comp }}
    {{ /comp }}

        {{ comp dashboard.Cell {
        title: "GS (Gateway Review) Number of individuals engaged with professional development or educational opportunities",
        flash: 'Sample',
    } }}
        {{ comp.BigNumber({
            number: sample.audience["GS (Gateway Review) Number of individuals engaged with professional development or educational opportunities"],
        }) }}
        {{ comp Controls }}
            {{ comp Popover }}
                TBC
            {{ /comp }}
        {{ /comp }}
    {{ /comp }}

       {{ comp dashboard.Cell {
        title: "GS (Gateway Review) Numbers of individuals provided with volunteering opportunities",
        flash: 'Sample',
    } }}
        {{ comp.BigNumber({
            number: sample.audience["GS (Gateway Review) Numbers of individuals provided with volunteering opportunities"],
        }) }}
        {{ comp Controls }}
            {{ comp Popover }}
                TBC
            {{ /comp }}
        {{ /comp }}
    {{ /comp }}

{{ /comp }} {{# dashboard.Frame #}}