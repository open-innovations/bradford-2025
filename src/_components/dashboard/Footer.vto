---
css: |
    [data-comp=dashboard-Cell] {
        .date {
            text-align: center;
            color: var(--date-fg, #dddddd);
        }
    }
---
{{ if upTo }}
    <p class='date'>
        <span class="text">up to {{ comp.Date({start:upTo}) }}</span>
    </p>
{{ /if }}