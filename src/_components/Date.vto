{{->
    if(typeof startDateFormat==="undefined") startDateFormat = dateFormat||((start.length==10 ? "d LLL" : "LLL") + ((new Date(start)).getFullYear() == (new Date(end)).getFullYear() ? "" : " y"));
    if(typeof endDateFormat==="undefined") endDateFormat = dateFormat||((end && end.length==10 ? "d LLL" : "LLL") + " y");
-}}
{{- if end -}}
<span class="date-range">{{ start |> date(startDateFormat)}} – {{ end |> date(endDateFormat)}}</span>
{{- else -}}
<span class="date">{{ start |> date(startDateFormat)}}</span>
{{- /if }}